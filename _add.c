#include "monty.h"

/**
  *_add - does
  *@head: the head of the list.
  *@argument: the argument to use.
  */
void _add(stack_t **head, unsigned int argument)
{
printf("%d\n", (*head)->n);
printf("%d\n", argument);
}
